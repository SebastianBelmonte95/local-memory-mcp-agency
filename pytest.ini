[pytest]
testpaths = tests
pythonpath = src
markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (real SQLite/FAISS, mocked Ollama)
    postgres: Tests specific to PostgreSQL backend
    sqlite: Tests specific to SQLite backend
addopts = --tb=short -q
